---
description: 星火项目常态化优化治理（每次改动必遵）
alwaysApply: true
---

# 优化治理

- 优先修热路径与用户可见问题；禁止顺手大重构无关模块。
- 写接口必须防重复提交（锁/去重）；读接口优先缓存，避免无意义打满 PUBG 配额。
- 改 Profile / Chat 等超大 Vue 文件时：只改必要区块，避免继续膨胀；能抽公共工具再抽。
- 涉及他人主页、排行榜跳转：必须按目标 `userId` 取数，禁止一律读当前登录用户。
- 生产相关：Nginx 需 `location ^~ /api/`；图片走 `/api/media`；历史赛季战力不得覆盖排行榜缓存。
- 交付前至少：相关前端 `npm run build` 或后端语法/启动冒烟；不臆测「线上已部署」。
